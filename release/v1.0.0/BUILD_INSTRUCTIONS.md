# 🛠️ BUILD_INSTRUCTIONS.md - Инструкции по сборке Kolibri v1.0.0

## 📋 Содержание

1. [Требования к системе](#требования-к-системе)
2. [Поддерживаемые платформы](#поддерживаемые-платформы)
3. [Установка зависимостей](#установка-зависимостей)
4. [Сборка проекта](#сборка-проекта)
5. [Запуск тестов](#запуск-тестов)
6. [Трубулесутинг](#трубулесутинг)
7. [Интеграция в ваш проект](#интеграция-в-ваш-проект)

---

## 📦 Требования к системе

### Обязательно

| Компонент | Версия | Причина |
|-----------|--------|---------|
| **CMake** | 3.15+ | Система сборки |
| **C Компилятор** | GCC 9+ или Clang 10+ | C11 standard |
| **GNU Make** | 4.0+ | Утилита сборки |
| **Python** | 3.8+ | Опционально для скриптов |

### Рекомендуется

- 4 GB RAM (минимум)
- 500 MB диского пространства
- macOS 10.15+, Ubuntu 18.04+, или CentOS 8+

---

## 🖥️ Поддерживаемые платформы

### ✅ Проверено и работает

| ОС | Версия | Компилятор | Статус |
|----|--------|-----------|--------|
| **macOS** | 11.0+ (Big Sur) | Apple Clang 13+ | ✅ Работает |
| **Ubuntu** | 18.04 LTS | GCC 9+ | ✅ Работает |
| **CentOS** | 8+ | GCC 9+ | ✅ Работает |
| **Debian** | 10+ | GCC 9+ | ✅ Работает |

### 🤔 Вероятно работает

- Windows (WSL2 с Ubuntu)
- Fedora 32+
- Arch Linux
- openSUSE 15.2+

### ❌ Не тестировалось

- Solaris
- BSD
- Embedded Linux (ARM Cortex-M)
- Windows MSVC (нужна кроссплатформа)

---

## 📥 Установка зависимостей

### macOS

```bash
# Установить Xcode Command Line Tools (если не установлены)
xcode-select --install

# Установить Homebrew (если не установлен)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Установить CMake
brew install cmake

# Проверить версии
cmake --version
gcc --version
```

**Ожидаемый вывод:**
```
cmake version 3.22.0
Apple clang version 13.0.0
```

### Ubuntu / Debian

```bash
# Обновить репозитории
sudo apt update

# Установить необходимые пакеты
sudo apt install -y \
  build-essential \
  cmake \
  git \
  wget \
  curl

# Проверить версии
cmake --version
gcc --version
make --version
```

**Ожидаемый вывод:**
```
cmake version 3.16.3+
gcc (Ubuntu 9.3.0-10ubuntu2) 9.3.0
GNU Make 4.2.1
```

### CentOS / RHEL

```bash
# Установить Development Tools
sudo yum groupinstall -y "Development Tools"

# Установить CMake
sudo yum install -y cmake3

# Линк для совместимости
sudo ln -s /usr/bin/cmake3 /usr/bin/cmake

# Проверить версии
cmake --version
gcc --version
```

---

## 🔨 Сборка проекта

### Быстрый старт (3 команды)

```bash
# 1. Перейти в папку build
cd release/v1.0.0/build

# 2. Создать проект CMake
cmake -S .. -B .

# 3. Скомпилировать
cmake --build . --config Release
```

**Ожидаемое время**: 5-15 секунд  
**Ожидаемый вывод**:
```
[ 33%] Building C object CMakeFiles/test_full_pipeline.dir/tests/test_full_pipeline.c.obj
[ 66%] Linking C executable test_full_pipeline
[100%] Built target test_full_pipeline
```

### Подробная сборка (с опциями)

```bash
# Перейти в папку build
cd release/v1.0.0/build

# Создать проект с опциями
cmake -S .. -B . \
  -DCMAKE_BUILD_TYPE=Release \
  -DCMAKE_C_COMPILER=gcc \
  -DCMAKE_C_FLAGS="-Wall -Wextra -O3"

# Скомпилировать с подробным выводом
cmake --build . --config Release --verbose
```

### Сборка в режиме отладки

```bash
# Для разработки с отладочной информацией
cmake -S .. -B . -DCMAKE_BUILD_TYPE=Debug

# Скомпилировать с отладочными символами
cmake --build . --config Debug
```

### Сборка с Address Sanitizer (выявление ошибок памяти)

```bash
# Для поиска утечек памяти
cmake -S .. -B . \
  -DCMAKE_BUILD_TYPE=Debug \
  -DCMAKE_C_FLAGS="-fsanitize=address -fno-omit-frame-pointer"

# Скомпилировать
cmake --build . --config Debug
```

---

## ✅ Запуск тестов

### Запуск основного теста (полный конвейер)

```bash
# Убедиться, что находитесь в папке build
cd release/v1.0.0/build

# Запустить тест
./test_full_pipeline
```

**Ожидаемый вывод (начало)**:
```
╔═══════════════════════════════════════════════════╗
║         🧪 ПОЛНЫЙ КОНВЕЙЕР KOLIBRI - ТЕСТ         ║
╚═══════════════════════════════════════════════════╝

[1] Создание исходных данных
  ✓ Размер: 7 bytes
  ✓ Данные (hex): 42 45 45 50 00 00 FF
```

**Ожидаемый вывод (конец)**:
```
[10] Проверка идентичности
  ✓ УСПЕХ: Данные идентичны!
  ✓ Размер: 7 bytes (совпадает)
  ✓ Содержимое: 100% идентично

✅ ВСЕ ТЕСТЫ ПРОЙДЕНЫ!
```

### Запуск теста Logical Memory

```bash
./test_logical_memory
```

**Ожидаемый результат**: Тест операций уровня 2

### Запуск теста Formula Logic

```bash
./test_formula_logic
```

**Ожидаемый результат**: Тест операций уровня 3

### Запуск всех тестов сразу

```bash
# Макрос для запуска всех
for test in test_* ; do
  echo "Running $test..."
  ./$test
  echo "---"
done
```

---

## 🔧 Трубулесутинг

### Проблема: CMake не найден

```bash
# Ошибка
zsh: command not found: cmake

# Решение для macOS
brew install cmake

# Решение для Ubuntu
sudo apt install cmake

# Проверка установки
cmake --version
```

### Проблема: Компилятор не найден

```bash
# Ошибка
error: gcc: No such file or directory

# Проверить наличие
gcc --version
clang --version

# Если GCC не установлен
sudo apt install build-essential  # Ubuntu
brew install gcc                   # macOS
```

### Проблема: Ошибка при компиляции

```bash
# Стандартная ошибка
error: 'LogicExpression' has no member named 'value'

# Решение
1. Проверить что вы в папке release/v1.0.0
2. Удалить кэш: rm -rf build/CMakeCache.txt build/CMakeFiles
3. Пересоздать проект: cmake -S .. -B .
4. Перекомпилировать: cmake --build .
```

### Проблема: Недостаточно прав

```bash
# Ошибка
Permission denied: ./test_full_pipeline

# Решение
chmod +x ./test_*
./test_full_pipeline
```

### Проблема: Out of memory

```bash
# Если во время компиляции выйдет ошибка памяти
# Включить подкачку (для слабых машин)
sudo fallocate -l 2G /swapfile  # Linux
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
```

### Проблема: Порт занят (при использовании API)

```bash
# В будущих версиях с API сервером
# Если видите: Address already in use

# Найти процесс
lsof -i :8080

# Убить процесс
kill -9 <PID>
```

### Полная переустановка

Если ничего не помогает:

```bash
# Очистить всё
cd release/v1.0.0
rm -rf build/*
rm -rf build_artifacts/*

# Пересоздать структуру
mkdir -p build

# Пересоздать проект
cd build
cmake -S .. -B .
cmake --build .

# Запустить тесты
./test_full_pipeline
```

---

## 🔗 Интеграция в ваш проект

### Метод 1: Копирование файлов

```bash
# 1. Скопировать исходные файлы
cp -r release/v1.0.0/src/* /path/to/your/project/

# 2. Скопировать тесты
cp -r release/v1.0.0/tests/* /path/to/your/tests/

# 3. Добавить в ваш CMakeLists.txt
```

### Метод 2: CMake включение

**Ваш CMakeLists.txt:**

```cmake
# Добавить Kolibri как подпроект
add_subdirectory(../release/v1.0.0 kolibri)

# Линковать ваш целевой файл
target_link_libraries(your_target kolibri)

# Включить хидеры
target_include_directories(your_target PRIVATE ../release/v1.0.0/src)
```

### Метод 3: Как библиотека

```bash
# В папке release/v1.0.0
cd build

# Скомпилировать как статическую библиотеку
cmake -S .. -B . -DBUILD_SHARED_LIBS=OFF
cmake --build .

# Скопировать библиотеку
cp libkolibri.a /usr/local/lib/
cp ../src/*.h /usr/local/include/

# В вашем коде
#include <formula_logic.h>
```

### Метод 4: Использование в C коде

```c
#include "formula_logic.h"
#include "logical_memory.h"
#include "decimal.h"

int main() {
    // Инициализировать память
    LogicalMemory* memory = lm_create_memory();
    
    // Использовать функции
    unsigned char data[] = {0x42, 0x45, 0x45, 0x50};
    char decimal[256];
    
    encode_to_decimal(data, 4, decimal);
    
    LogicExpression expr = lm_logic_constant(decimal);
    
    // Очистить
    lm_destroy_memory(memory);
    
    return 0;
}
```

**Компиляция:**

```bash
gcc -o my_program my_program.c \
  release/v1.0.0/src/formula_logic.c \
  release/v1.0.0/src/logical_memory.c \
  release/v1.0.0/src/decimal.c \
  -I release/v1.0.0/src \
  -std=c11 -Wall
```

---

## 📊 Проверка установки

Вот скрипт для проверки всего:

```bash
#!/bin/bash

echo "=== Проверка установки Kolibri ==="

# Проверка CMake
if command -v cmake &> /dev/null; then
    echo "✅ CMake: $(cmake --version | head -1)"
else
    echo "❌ CMake не найден"
    exit 1
fi

# Проверка GCC
if command -v gcc &> /dev/null; then
    echo "✅ GCC: $(gcc --version | head -1)"
else
    echo "❌ GCC не найден"
    exit 1
fi

# Проверка Make
if command -v make &> /dev/null; then
    echo "✅ Make: $(make --version | head -1)"
else
    echo "❌ Make не найден"
    exit 1
fi

# Проверка файлов
if [ -f "release/v1.0.0/build/CMakeLists.txt" ]; then
    echo "✅ CMakeLists.txt найден"
else
    echo "❌ CMakeLists.txt не найден"
    exit 1
fi

# Сборка
echo -e "\n=== Сборка ==="
cd release/v1.0.0/build
cmake -S .. -B . 2>/dev/null
cmake --build . 2>/dev/null

if [ -f "./test_full_pipeline" ]; then
    echo "✅ Сборка успешна"
else
    echo "❌ Сборка не удалась"
    exit 1
fi

# Запуск теста
echo -e "\n=== Запуск теста ==="
./test_full_pipeline | head -20
echo "..."
./test_full_pipeline | tail -5

echo -e "\n✅ Установка проверена!"
```

Сохраните как `check_kolibri.sh` и запустите:

```bash
chmod +x check_kolibri.sh
./check_kolibri.sh
```

---

## 📚 Дополнительные ресурсы

### Документация

- `README.md` - Обзор v1.0.0
- `RELEASE_NOTES.md` - Что нового
- `docs/FULL_PIPELINE_ARCHITECTURE.md` - Архитектура

### Примеры

- `tests/test_full_pipeline.c` - Полный конвейер
- `tests/test_logical_memory.c` - Уровень 2
- `tests/test_formula_logic.c` - Уровень 3

### Справочник API

**Decimal (Уровень 1)**:
```c
int encode_to_decimal(unsigned char* data, size_t len, char* output);
int decode_from_decimal(const char* decimal_str, unsigned char* output);
```

**Logical Memory (Уровень 2)**:
```c
LogicalMemory* lm_create_memory();
void lm_destroy_memory(LogicalMemory* memory);
LogicExpression lm_logic_constant(const char* value);
```

**Formula Logic (Уровень 3)**:
```c
MetaFormula mf_create_formula(MetaFormulaType type, ...);
LogicExpression mf_execute(MetaFormula* formula, LogicalMemory* memory);
```

---

## ❓ FAQ

### Вопрос: Зачем нужна сборка?

**Ответ**: Kolibri написан на C11 для производительности. Сборка требуется для компиляции в машинный код.

### Вопрос: Какой компилятор использовать?

**Ответ**: GCC или Clang. На macOS автоматически используется Apple Clang. На Linux рекомендуется GCC.

### Вопрос: Могу ли я использовать MSVC?

**Ответ**: Теоретически да, но не тестировалось. Используйте WSL2 с Ubuntu для надежности.

### Вопрос: Какой режим Debug vs Release?

**Ответ**: 
- **Debug**: Для разработки (медленнее, с отладкой)
- **Release**: Для production (быстрее, без отладки)

### Вопрос: Какой режим использовать?

**Ответ**: Release для тестирования, Debug для отладки.

---

## ✨ Следующие шаги

После успешной сборки:

1. ✅ Запустите `test_full_pipeline`
2. 📖 Прочитайте `README.md`
3. 📚 Изучите архитектуру в `docs/`
4. 💻 Интегрируйте в ваш проект
5. 🚀 Начните разработку

---

*Инструкции последний раз обновлены: 12 ноября 2025 г.*

**Приятной сборки! 🎉**
